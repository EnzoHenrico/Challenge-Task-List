// User input
const input = prompt('-> ')

const messages = {
    help: `Task Table commands:

    - list:
        - pendent
        - all
    - add:
        - new
    - change:
        - status
        - delete`,
}

console.log(input)
